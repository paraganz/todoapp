name: first workflow
on: workflow_dispatch
jobs:
  my-first-job:
    runs-on: ubuntu-latest
    steps:
      - run: echo "helloss"
  my-second-job:
    env: 
      firstvar: "parag"
    runs-on: macos-latest
    steps:
    - run: echo "by worlds"
    - run: echo "${{ inputs.debug}}"
    - run: echo "{{ $firstvars }}"
    - run: echo "{{ $var.debugg }}"