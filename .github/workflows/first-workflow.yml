name: first workflow
on: 
  workflow_dispatch:
    inputs:
      debug:
        type: string

jobs:
  my-first-job:
    runs-on: ubuntu-latest
    steps:
      - run: echo "helloss"
  my-second-job:
    env: 
      firstvar: "parag"
    runs-on: macos-latest
    steps:
      - run: echo "by worlds"
      - run: echo "${{ inputs.debug }}"
      - run: echo "{{ $firstvar }}"
      - run: echo "${{ vars.debugg }}"